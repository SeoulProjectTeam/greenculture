# (선택) Spring에서 환경변수로 DB 설정을 덮어쓸 때 사용
# backend/src/main/resources/application.properties 에서 기본값이 있으므로 없어도 동작합니다.

SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/greenculture
SPRING_DATASOURCE_USERNAME=greenculture
SPRING_DATASOURCE_PASSWORD=greenculture
SPRING_JPA_DDL_AUTO=update

